// Componente Sparkline retirado: agregaba ruido visual sin un valor narrativo
// claro para el cliente (la subida podía ser crecimiento del padrón, no
// performance política, y la lectura era ambigua). El endpoint
// /metricas/{id}/serie-historica/{geo_id} queda disponible en el backend por
// si lo queremos reutilizar en otra vista.
export {}
